<li id="comment_<%=comment.id %>">
  <%= comment.comment %>,
  <%= time_ago_in_words(comment.created_at) %> ago
  <%= link_to "삭제", [comment.post, comment], method: :delete, remote: true, data: { confirm: "정말 삭제하시겠습니까?" } %>
</li>